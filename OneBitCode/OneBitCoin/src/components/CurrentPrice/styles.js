import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center"
    },
    currentPrice: {
        color: "#32CD32",
        fontSize: 36,
        fontWeight: "bold",
        paddingTop: 20
    },
    textPrice: {
        color: "#fff",
        fontSize: 16,
        fontWeight: "bold",
        paddingTop: 10,
        paddingBottom: 20
    }
})

export default styles